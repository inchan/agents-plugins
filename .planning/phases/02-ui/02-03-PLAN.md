---
phase: 02-ui
plan: "03"
type: execute
wave: 2
depends_on: ["02-01"]
files_modified:
  - plugins/wf/commands/ticket-workflow.md
  - plugins/wf/skills/ticket-workflow/references/phase-orchestrator.md
autonomous: true
requirements: [UIBR-03, UIBR-04]

must_haves:
  truths:
    - "가용 브라우저 도구가 2개 이상이면 Agent로 병렬 디스패치하여 크로스체크한다"
    - "두 도구의 재현 결과가 일치하면 신뢰도 HIGH, 불일치면 MEDIUM으로 판정되고 두 결과 모두 evidence에 포함된다"
    - "도구가 1개만 가용하면 단독 실행이 정상 경로이다 (크로스체크는 선택적 최적화)"
    - "Agent Registry에 browser-reproducer가 등록되고 Phase 1 EVIDENCE에서 UI 티켓 시 디스패치 된다"
    - "phase-orchestrator에 UI 경로의 데이터 플로우(screenshot_references)가 EVIDENCE → VERIFY로 전달된다"
  artifacts:
    - path: "plugins/wf/commands/ticket-workflow.md"
      provides: "browser-reproducer 에이전트가 Agent Registry에 등록되고 Phase 1에서 UI 디스패치"
      contains: "browser-reproducer"
    - path: "plugins/wf/skills/ticket-workflow/references/phase-orchestrator.md"
      provides: "UI 경로 데이터 플로우 + 병렬 크로스체크 규칙이 정의된 오케스트레이터"
      contains: "cross-check\|screenshot_references"
  key_links:
    - from: "plugins/wf/commands/ticket-workflow.md"
      to: "plugins/wf/agents/browser-reproducer.md"
      via: "Agent Registry에 등록 + Phase 1 EVIDENCE에서 UI 디스패치"
      pattern: "browser-reproducer"
    - from: "plugins/wf/skills/ticket-workflow/references/phase-orchestrator.md"
      to: "plugins/wf/skills/ticket-workflow/references/phases/verification.md"
      via: "phase_outputs[EVIDENCE].screenshot_references가 VERIFY에 전달"
      pattern: "screenshot_references"
---

<objective>
병렬 크로스체크 전략을 phase-orchestrator에 정의하고, ticket-workflow 커맨드에 browser-reproducer를 Agent Registry에 등록하여 UI 경로를 완전히 연결한다.

Purpose: 가용한 브라우저 도구가 2개 이상이면 병렬로 재현하여 신뢰도를 높이고, UI 경로의 데이터 플로우가 EVIDENCE에서 VERIFY까지 끊김 없이 전달되어야 한다.
Output: 업데이트된 ticket-workflow.md (Agent Registry + UI 디스패치), 업데이트된 phase-orchestrator.md (크로스체크 + 데이터 플로우)
</objective>

<execution_context>
@/Users/chans/.claude/get-shit-done/workflows/execute-plan.md
@/Users/chans/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/02-ui/02-CONTEXT.md
@.planning/phases/02-ui/02-RESEARCH.md

@plugins/wf/commands/ticket-workflow.md
@plugins/wf/skills/ticket-workflow/references/phase-orchestrator.md
@plugins/wf/agents/browser-reproducer.md

<interfaces>
<!-- ticket-workflow.md Agent Registry 현재 상태 -->
| Agent | File | Used In | Tools |
|-------|------|---------|-------|
| evidence-searcher | agents/evidence-searcher.md | Phase 1 (x2 parallel) | Glob, Grep, Read, Bash |
| bug-tracer | agents/bug-tracer.md | Phase 3 (x2-3 parallel) | Glob, Grep, Read, Bash |
| planner | agents/planner.md | Phase 4 | Read, Glob, Grep |
| implementer | agents/implementer.md | Phase 5 | Bash, Read, Edit, Write, Glob, Grep |
| verifier | agents/verifier.md | Phase 6 | Bash, Read, Glob, Grep |
<!-- browser-reproducer 누락 — 추가 필요 -->

<!-- phase-orchestrator.md Rule 6: Type-Dependent Branching -->
UI 분기 규칙이 정의되어 있으나, 구체적인 데이터 플로우(screenshot_references 전달)는 미정의 상태

<!-- 병렬 크로스체크 패턴 (02-RESEARCH.md) -->
가용 도구 2개 이상:
  Agent A: Playwright MCP로 재현
  Agent B: Chrome DevTools로 재현
  결과 일치 → confidence HIGH
  결과 불일치 → confidence MEDIUM, 두 결과 모두 evidence 포함
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: ticket-workflow.md에 browser-reproducer Agent Registry 등록 + UI 디스패치</name>
  <files>plugins/wf/commands/ticket-workflow.md</files>
  <action>
ticket-workflow.md를 Read로 읽고 두 곳을 수정한다:

**1. Agent Registry 테이블에 browser-reproducer 추가:**
기존 테이블 마지막 행(verifier) 아래에 추가:
```
| `browser-reproducer` | `agents/browser-reproducer.md` | Phase 1 (UI tickets), Phase 6 (UI verify) | Bash, Read, Glob, Grep, WebFetch |
```

**2. Phase 1: EVIDENCE 섹션에 UI 티켓 시 browser-reproducer 디스패치 추가:**
기존 2x evidence-searcher 디스패치 블록 아래에 추가:
```
After evidence-searcher agents return:
  If ticket_type == "ui" OR classification hint suggests UI:
    Dispatch browser-reproducer agent:

    Agent C (browser-reproducer):
      Task: "Reproduce UI bug at <url> with viewport <viewport>: <reproduction_steps>"
      Tools: Bash, Read, Glob, Grep, WebFetch + available MCP tools
      Timeout: 19 minutes 59 seconds

    Merge browser-reproducer output into evidence_report:
      evidence_report.screenshot_references = agent_c.screenshots
      evidence_report.browser_method = agent_c.method
      evidence_report.reproduction_status = agent_c.reproduction_status
```

**3. Phase 6: VERIFY 섹션에 UI 비교 검증 참조 추가:**
기존 Phase 6 VERIFY 설명에 한 줄 추가:
```
For UI tickets: Also run browser-based Before/After comparison. See `verification.md` Step 2 UI section.
```

기존 내용(Phase 2-5 설명, Retry Control Flow, Orchestrator Reference)은 변경하지 않는다.
  </action>
  <verify>
    <automated>grep -c "browser-reproducer" plugins/wf/commands/ticket-workflow.md | awk '{if ($1 >= 3) print "PASS: browser-reproducer registered and dispatched"; else print "FAIL: browser-reproducer not fully integrated"}'</automated>
    추가 검증: grep "browser-reproducer" plugins/wf/commands/ticket-workflow.md (Agent Registry + Phase 1 디스패치 + Phase 6 참조 3곳)
  </verify>
  <done>ticket-workflow.md Agent Registry에 browser-reproducer가 등록되고, Phase 1 EVIDENCE에서 UI 티켓 시 디스패치, Phase 6에서 UI 비교 참조가 추가됨</done>
</task>

<task type="auto">
  <name>Task 2: phase-orchestrator.md에 병렬 크로스체크 + UI 데이터 플로우 정의</name>
  <files>plugins/wf/skills/ticket-workflow/references/phase-orchestrator.md</files>
  <action>
phase-orchestrator.md를 Read로 읽고 두 가지를 추가한다:

**1. 병렬 크로스체크 규칙 (새 섹션 또는 기존 Rule 6 확장):**
Rule 6 "Type-Dependent Branching" 근처에 "Browser Cross-Check Strategy" 하위 섹션 추가:

```
### Browser Cross-Check Strategy

가용 도구 수에 따른 실행 전략:

| Available Tools | Strategy | Execution |
|-----------------|----------|-----------|
| 0 | code-analysis only | 코드 수준 분석 (항상 가용) |
| 1 | single-tool | 해당 도구로 단독 실행 |
| 2+ | parallel cross-check | Agent 병렬 디스패치, 결과 비교 |

병렬 크로스체크 절차:
1. 가용 도구 2개 이상 감지 시 Agent 도구로 병렬 디스패치
2. 각 Agent는 동일한 reproduction steps를 서로 다른 도구로 실행
3. 결과 집계:
   - 두 결과 일치 → reproduction_confidence = "HIGH"
   - 두 결과 불일치 → reproduction_confidence = "MEDIUM"
     → [WARN] [BROWSER] -- Cross-check mismatch: tools disagree
     → 두 결과 모두 evidence_report에 포함
4. 크로스체크는 선택적 최적화 — 필수가 아님
```

**2. UI 데이터 플로우 (screenshot_references 전달):**
기존 Inter-Phase Data Flow 또는 Phase Output Contracts 섹션에 추가:

```
### UI Screenshot Data Flow

EVIDENCE → VERIFY 전달 필드 (ticket_type == "ui" 전용):
  phase_outputs["EVIDENCE"].screenshot_references = [
    { type: "before_full", path: "/tmp/wf_{ticket_id}/before_full.png" },
    { type: "before_element", path: "/tmp/wf_{ticket_id}/before_element.png" }
  ]
  phase_outputs["EVIDENCE"].browser_method = "browser:playwright_mcp" | ...
  phase_outputs["EVIDENCE"].reproduction_status = "success" | "partial" | "failed"

VERIFY에서 위 필드를 참조하여:
  - 동일 도구/URL/steps로 After 스크린샷 캡처
  - Before/After 비교 실행
  - visual_comparison 결과를 verification_report에 포함
```

기존 phase-orchestrator.md의 State Machine, Phase Definitions, Control Flow Rules, Retry Logic 등은 변경하지 않는다.
  </action>
  <verify>
    <automated>grep -c "cross.check\|Cross.Check\|screenshot_references\|parallel.*dispatch\|reproduction_confidence" plugins/wf/skills/ticket-workflow/references/phase-orchestrator.md | awk '{if ($1 >= 4) print "PASS: cross-check and data flow defined"; else print "FAIL: missing definitions"}'</automated>
    추가 검증: grep -c "screenshot_references" plugins/wf/skills/ticket-workflow/references/phase-orchestrator.md (EVIDENCE → VERIFY 데이터 전달 정의)
  </verify>
  <done>phase-orchestrator.md에 병렬 크로스체크 전략 테이블과 UI screenshot 데이터 플로우(EVIDENCE → VERIFY)가 정의됨</done>
</task>

</tasks>

<verification>
- ticket-workflow.md Agent Registry에 browser-reproducer가 등록됨
- Phase 1 EVIDENCE에서 UI 티켓 시 browser-reproducer 에이전트 디스패치가 명시됨
- phase-orchestrator.md에 병렬 크로스체크 전략(0/1/2+ 도구별)이 테이블로 정의됨
- phase-orchestrator.md에 screenshot_references가 EVIDENCE → VERIFY로 전달되는 데이터 플로우가 정의됨
- 크로스체크가 "선택적 최적화"로 명시되어 도구 1개일 때도 정상 동작함
</verification>

<success_criteria>
- `grep -c "browser-reproducer" plugins/wf/commands/ticket-workflow.md` >= 3
- `grep -c "cross.check\|Cross.Check" plugins/wf/skills/ticket-workflow/references/phase-orchestrator.md` >= 2
- `grep -c "screenshot_references" plugins/wf/skills/ticket-workflow/references/phase-orchestrator.md` >= 2
- `grep -c "reproduction_confidence" plugins/wf/skills/ticket-workflow/references/phase-orchestrator.md` >= 1
</success_criteria>

<output>
After completion, create `.planning/phases/02-ui/02-03-SUMMARY.md`
</output>
