---
phase: 02-ui
plan: "02"
type: execute
wave: 2
depends_on: ["02-01"]
files_modified:
  - plugins/wf/skills/ticket-workflow/references/phases/verification.md
  - plugins/wf/agents/verifier.md
autonomous: true
requirements: [UIBR-02]

must_haves:
  truths:
    - "수정 후 동일 URL/동일 reproduction steps로 After 스크린샷(전체 페이지 + 요소)이 캡처된다"
    - "Before/After 스크린샷이 maxDiffPixels 기반으로 비교되어 시각적 차이 유무가 판정된다"
    - "비교 결과가 visual_comparison JSON 구조로 출력된다 (status, before/after paths, differences, diff_pixels)"
    - "Visual 검증 실패는 E_VERIFY로 재시도 트리거이지, 즉각 FAILED가 아니다"
  artifacts:
    - path: "plugins/wf/skills/ticket-workflow/references/phases/verification.md"
      provides: "UI 티켓용 Before/After 비교 검증 절차가 활성화된 verification"
      contains: "visual_comparison"
    - path: "plugins/wf/agents/verifier.md"
      provides: "UI 검증 시 browser-reproducer를 활용하는 verifier 에이전트"
  key_links:
    - from: "plugins/wf/skills/ticket-workflow/references/phases/verification.md"
      to: "plugins/wf/agents/browser-reproducer.md"
      via: "After 스크린샷 캡처를 위한 browser-reproducer 재실행"
      pattern: "browser-reproducer"
    - from: "plugins/wf/skills/ticket-workflow/references/phases/verification.md"
      to: "plugins/wf/skills/ticket-workflow/references/browser-automation.md"
      via: "Into Verification Report JSON 구조 참조"
      pattern: "visual_comparison"
---

<objective>
verification.md에 UI 티켓용 After 스크린샷 캡처 및 Before/After 비교 검증 절차를 활성화한다.

Purpose: 수정 완료 후 동일 조건으로 재캡처하여 시각적 변화를 정량적으로 비교할 수 있어야 한다.
Output: 활성화된 verification.md (After 캡처 + 비교 로직), 업데이트된 verifier.md
</objective>

<execution_context>
@/Users/chans/.claude/get-shit-done/workflows/execute-plan.md
@/Users/chans/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/02-ui/02-CONTEXT.md
@.planning/phases/02-ui/02-RESEARCH.md

@plugins/wf/agents/browser-reproducer.md
@plugins/wf/agents/verifier.md
@plugins/wf/skills/ticket-workflow/references/phases/verification.md
@plugins/wf/skills/ticket-workflow/references/browser-automation.md

<interfaces>
<!-- Plan 02-01에서 완성될 browser-reproducer 에이전트의 출력 구조 -->

browser-reproducer OUTPUT:
  reproduction_status: success | partial | failed
  method: browser:playwright_mcp | browser:chrome_devtools | browser:agent_browser | code-analysis
  screenshots: [before_full_path, before_element_path]
  dom_evidence: DOM 상태
  console_errors: 콘솔 에러 목록

Verification Report visual_comparison (browser-automation.md에 정의됨):
{
  "visual_comparison": {
    "status": "match" | "mismatch",
    "before_screenshot": "/tmp/wf_{ticket_id}/before_full.png",
    "after_screenshot": "/tmp/wf_{ticket_id}/after_full.png",
    "differences": [...],
    "diff_pixels": 0,
    "max_diff_pixels": 200,
    "assertions_passed": 3,
    "assertions_failed": 0
  }
}

maxDiffPixels 임계값 (Claude 재량 — RESEARCH.md 권장값):
  - element 스크린샷: 100px
  - full page 스크린샷: 500px
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: verification.md에 UI Before/After 비교 검증 절차 활성화</name>
  <files>plugins/wf/skills/ticket-workflow/references/phases/verification.md</files>
  <action>
verification.md의 "Step 2: Type-Specific Verification" > "For UI Tickets" 섹션을 확장한다. 기존의 개략적 Playwright/Chrome DevTools/agent-browser 목록을 구체적인 After 캡처 + 비교 절차로 교체:

**After 스크린샷 캡처 절차:**
1. Phase 1 EVIDENCE에서 저장된 `phase_outputs["EVIDENCE"].screenshot_references`를 읽어 Before 경로 확인
2. 동일한 primary_tool (EVIDENCE 단계와 같은 도구)로 동일 URL에 navigate
3. 동일 reproduction steps 재실행 (동일 viewport)
4. `wait_for_load_state("networkidle")` 후 After 스크린샷 캡처:
   - 전체 페이지: /tmp/wf_{ticket_id}/after_full.png
   - 영향받는 요소: /tmp/wf_{ticket_id}/after_element.png
5. 도구 실패 시 Plan 02-01에서 구현한 폴백 체인 동일하게 적용

**Before/After 비교 로직:**
- maxDiffPixels 비교: element 스크린샷 기본 100px, full page 기본 500px
- DOM 구조 비교: evaluate로 관련 요소의 DOM 상태 직렬화 비교
- 계산된 스타일 비교: getComputedStyle 결과 JSON 비교 (변경된 속성 유무)
- 세 가지 비교 중 하나라도 개선(변화)이 확인되면 visual_comparison.status = "match"

**비교 결과 출력:** browser-automation.md의 "Into Verification Report" JSON 구조 그대로:
```
visual_comparison: {
  status: "match" | "mismatch",
  before_screenshot: path,
  after_screenshot: path,
  differences: [...],
  diff_pixels: N,
  max_diff_pixels: 200,
  assertions_passed: N,
  assertions_failed: N
}
```

**실패 처리:**
- visual_comparison.status == "mismatch"이면 E_VERIFY → 재시도 트리거 (즉각 FAILED 아님)
- 스크린샷 캡처 자체가 실패하면 → DOM/스타일 기반 비교로 폴백, `[WARN] [VERIFY]` 로그

기존의 `<!-- Phase 2 UI: browser-reproducer 통합 예정 -->` 주석을 제거한다.
기존의 Non-UI 검증 절차, 재시도 전략, 에러 처리 등 나머지 내용은 모두 유지한다.
  </action>
  <verify>
    <automated>grep -c "visual_comparison\|after_screenshot\|before_screenshot\|maxDiffPixels\|max_diff_pixels" plugins/wf/skills/ticket-workflow/references/phases/verification.md | awk '{if ($1 >= 4) print "PASS: visual comparison fields present"; else print "FAIL: missing visual comparison"}'</automated>
    추가 검증: grep -c "Phase 2" plugins/wf/skills/ticket-workflow/references/phases/verification.md (Phase 2 스텁 주석 제거 확인 — 0이어야 함)
  </verify>
  <done>verification.md에 After 스크린샷 캡처 + Before/After maxDiffPixels 비교 + visual_comparison JSON 출력이 구체적으로 정의되고, Phase 2 스텁 주석이 제거됨</done>
</task>

<task type="auto">
  <name>Task 2: verifier.md에 UI 검증 시 browser-reproducer 활용 로직 추가</name>
  <files>plugins/wf/agents/verifier.md</files>
  <action>
verifier.md 에이전트 정의에 UI 티켓 검증 경로를 추가한다:

1. 기존 verifier.md를 Read로 읽어 현재 구조 파악
2. 에이전트 본문에 "UI Ticket Verification" 섹션 추가:
   - ticket_type == "ui"일 때 추가 검증 단계:
     a. phase_outputs["EVIDENCE"]에서 Before 스크린샷 경로 확인
     b. browser-reproducer 에이전트의 도구 감지 결과(EVIDENCE 단계에서 캐싱됨)를 재사용
     c. 동일 도구/URL/steps로 After 스크린샷 캡처
     d. Before/After 비교 실행 (verification.md 절차 참조)
     e. visual_comparison 결과를 verification_report에 포함
   - 비교 실패 시 E_VERIFY로 재시도 트리거
3. YAML frontmatter의 tools 필드에 WebFetch가 없으면 추가 (브라우저 관련 검증에 필요할 수 있음)
4. 기존 Non-UI 검증 로직은 그대로 유지

**주의:** verifier.md의 기존 구조(테스트 실행, 결과 분석, 재시도 컨텍스트 처리)는 변경하지 않는다. UI 분기 섹션만 추가한다.
  </action>
  <verify>
    <automated>grep -c "UI\|visual_comparison\|browser-reproducer\|after_screenshot" plugins/wf/agents/verifier.md | awk '{if ($1 >= 3) print "PASS: UI verification added"; else print "FAIL: UI verification missing"}'</automated>
  </verify>
  <done>verifier.md에 UI 티켓용 After 스크린샷 캡처 + Before/After 비교 분기가 추가되고, 기존 Non-UI 로직이 유지됨</done>
</task>

</tasks>

<verification>
- verification.md에 After 스크린샷 캡처 절차가 구체적으로 정의됨
- Before/After 비교가 maxDiffPixels + DOM + 스타일 3가지 방법으로 수행됨
- visual_comparison JSON 출력이 browser-automation.md의 "Into Verification Report" 구조와 일치함
- verifier.md에 UI 분기 로직이 추가됨
- 모든 "Phase 2" 스텁 주석이 제거됨
</verification>

<success_criteria>
- `grep -c "visual_comparison" plugins/wf/skills/ticket-workflow/references/phases/verification.md` >= 2
- `grep -c "after_screenshot" plugins/wf/skills/ticket-workflow/references/phases/verification.md` >= 1
- `grep -c "Phase 2" plugins/wf/skills/ticket-workflow/references/phases/verification.md` == 0
- `grep -c "browser-reproducer" plugins/wf/agents/verifier.md` >= 1
</success_criteria>

<output>
After completion, create `.planning/phases/02-ui/02-02-SUMMARY.md`
</output>
