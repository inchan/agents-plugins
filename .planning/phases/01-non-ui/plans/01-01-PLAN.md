---
phase: 01-non-ui
plan: 01
slug: structure-merge
type: execute
wave: 1
depends_on: []
files_modified:
  - plugins/wf/.claude-plugin/plugin.json
  - plugins/wf/agents/evidence-searcher.md
  - plugins/wf/agents/bug-tracer.md
  - plugins/wf/agents/planner.md
  - plugins/wf/agents/implementer.md
  - plugins/wf/agents/verifier.md
  - plugins/wf/agents/browser-reproducer.md
  - plugins/wf/skills/ticket-workflow/references/phase-orchestrator.md
  - plugins/wf/skills/ticket-workflow/references/classification.md
  - plugins/wf/skills/ticket-workflow/references/scoring.md
  - plugins/wf/skills/ticket-workflow/references/workflow-logger.md
  - plugins/wf/skills/ticket-workflow/references/browser-automation.md
  - plugins/wf/skills/ticket-workflow/references/phases/evidence-collection.md
  - plugins/wf/skills/ticket-workflow/references/phases/exploration.md
  - plugins/wf/skills/ticket-workflow/references/phases/planning.md
  - plugins/wf/skills/ticket-workflow/references/phases/implementation.md
  - plugins/wf/skills/ticket-workflow/references/phases/verification.md
autonomous: true
requirements: [PLUG-01, PLUG-02, PLUG-03]

must_haves:
  truths:
    - "plugins/wf/ 디렉토리에 .claude-plugin/plugin.json, agents/, skills/, references/ 표준 구조가 존재한다"
    - "v0.2의 6개 에이전트 .md 파일이 plugins/wf/agents/에 존재한다"
    - "v0.2의 references/ 8개 파일이 plugins/wf/skills/ticket-workflow/references/에 존재한다"
    - "plugins/ticket-workflow/ 구 경로가 완전히 삭제되었다 (lib/ Python 포함)"
    - "Python lib/*.py 파일이 프로젝트 어디에도 존재하지 않는다"
  artifacts:
    - path: "plugins/wf/.claude-plugin/plugin.json"
      provides: "플러그인 등록 메타데이터"
      contains: "wf"
    - path: "plugins/wf/agents/evidence-searcher.md"
      provides: "증거 수집 에이전트 정의"
    - path: "plugins/wf/agents/verifier.md"
      provides: "검증 에이전트 정의"
  key_links:
    - from: "plugins/wf/.claude-plugin/plugin.json"
      to: "plugins/wf/skills/ticket-workflow/SKILL.md"
      via: "플러그인 등록 → 스킬 진입점"
    - from: "plugins/wf/skills/ticket-workflow/SKILL.md"
      to: "plugins/wf/skills/ticket-workflow/references/"
      via: "references 링크"
      pattern: "references/"
---

<objective>
v0.2(`plugins/ticket-workflow/`)의 에이전트, references, 스키마를 `plugins/wf/`로 이전하고, Python lib/를 제거하여 단일 플러그인 구조를 완성한다.

Purpose: v0.2/v0.3 두 경로를 하나로 통합하여 단일 진실의 원천(single source of truth)을 만든다.
Output: plugins/wf/ 디렉토리에 표준 플러그인 구조 완성 + plugins/ticket-workflow/ 삭제
</objective>

<context>
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/phases/01-non-ui/01-CONTEXT.md
@.planning/phases/01-non-ui/01-RESEARCH.md
@plugins/ticket-workflow/ (v0.2 소스 — 이전 대상)
@plugins/wf/skills/ticket-workflow/SKILL.md (v0.3 — base로 유지)
@plugins/agent-delegation/.claude-plugin/plugin.json (plugin.json 구조 레퍼런스)
</context>

<tasks>

<task type="auto">
  <name>Task 1: v0.2 에셋을 plugins/wf/로 이전 + plugin.json 생성</name>
  <files>
    plugins/wf/.claude-plugin/plugin.json,
    plugins/wf/agents/evidence-searcher.md,
    plugins/wf/agents/evidence-collector.md,
    plugins/wf/agents/bug-tracer.md,
    plugins/wf/agents/planner.md,
    plugins/wf/agents/implementer.md,
    plugins/wf/agents/verifier.md,
    plugins/wf/agents/browser-reproducer.md,
    plugins/wf/skills/ticket-workflow/references/phase-orchestrator.md,
    plugins/wf/skills/ticket-workflow/references/classification.md,
    plugins/wf/skills/ticket-workflow/references/scoring.md,
    plugins/wf/skills/ticket-workflow/references/workflow-logger.md,
    plugins/wf/skills/ticket-workflow/references/browser-automation.md,
    plugins/wf/skills/ticket-workflow/references/phases/evidence-collection.md,
    plugins/wf/skills/ticket-workflow/references/phases/exploration.md,
    plugins/wf/skills/ticket-workflow/references/phases/planning.md,
    plugins/wf/skills/ticket-workflow/references/phases/implementation.md,
    plugins/wf/skills/ticket-workflow/references/phases/verification.md
  </files>
  <action>
    1. `plugins/wf/.claude-plugin/plugin.json` 생성. agent-delegation의 plugin.json과 동일 구조로:
       ```json
       {
         "name": "wf",
         "version": "1.0.0",
         "description": "Automated bug ticket workflow: evidence collection, classification, exploration, planning, implementation, and verification with UI/non-UI classification",
         "author": { "name": "inchan", "email": "inchan@augmentedwe.com" }
       }
       ```

    2. `plugins/ticket-workflow/agents/` 내 7개 .md 파일을 `plugins/wf/agents/`로 복사:
       - evidence-searcher.md, evidence-collector.md, bug-tracer.md, planner.md, implementer.md, verifier.md, browser-reproducer.md

    3. `plugins/ticket-workflow/skills/ticket-workflow/references/` 내 모든 .md 파일을 `plugins/wf/skills/ticket-workflow/references/`로 복사:
       - phase-orchestrator.md, classification.md, scoring.md, workflow-logger.md, browser-automation.md
       - phases/evidence-collection.md, phases/exploration.md, phases/planning.md, phases/implementation.md, phases/verification.md

    4. 복사된 references/*.md 파일에서 `lib/*.py` 참조를 모두 제거:
       - `lib/browser_automation.py` 등 Python 모듈 참조를 "Claude Code 도구로 직접 실행" 설명으로 대체
       - `../../lib/schemas/classification.md` 경로를 `references/classification.md` 내부 섹션으로 통합
       - evidence-collection.md에서 `BrowserAutomation 모듈` 참조를 "Phase 2에서 구현 예정" 주석으로 변경

    5. browser-reproducer.md는 Phase 2용 스텁으로 처리:
       - YAML frontmatter (name, description, tools, model, color) 유지
       - 본문을 "Phase 2에서 Playwright MCP / Chrome DevTools 통합 예정" 안내로 교체
  </action>
  <verify>
    find plugins/wf -type f -name "*.md" -o -name "*.json" | sort && echo "---" && test -f plugins/wf/.claude-plugin/plugin.json && echo "plugin.json OK" && test -f plugins/wf/agents/evidence-searcher.md && echo "agents OK" && test -f plugins/wf/skills/ticket-workflow/references/phase-orchestrator.md && echo "references OK" && test -d plugins/wf/skills/ticket-workflow/references/phases && echo "phases OK"
  </verify>
  <done>plugins/wf/ 디렉토리에 plugin.json + 7개 에이전트 + 10개 references 파일이 존재하고, Python lib/ 참조가 모두 제거됨</done>
</task>

<task type="auto">
  <name>Task 2: plugins/ticket-workflow/ 구 경로 삭제 + lib/schemas/ 통합</name>
  <files>
    plugins/ticket-workflow/ (삭제 대상)
  </files>
  <action>
    1. `plugins/ticket-workflow/lib/schemas/classification.md`의 유용한 내용(분류 출력 JSON 스키마)을 `plugins/wf/skills/ticket-workflow/references/classification.md` 하단에 "## Classification Output Schema" 섹션으로 병합

    2. `plugins/ticket-workflow/` 디렉토리 전체를 삭제:
       - `rm -rf plugins/ticket-workflow/`
       - 이 디렉토리에는 lib/*.py (Python), __pycache__/, test_*.py 등이 포함되어 있으나 모두 제거 대상
       - v0.2 SKILL.md(130줄)는 v0.3(377줄)이 대체하므로 삭제해도 무방

    3. 삭제 후 확인: `ls plugins/ticket-workflow 2>&1`이 "No such file or directory" 반환
  </action>
  <verify>
    ls plugins/ticket-workflow 2>&1 | grep -q "No such file" && echo "OLD PATH REMOVED OK" && find plugins -name "*.py" | wc -l | grep -q "0" && echo "NO PYTHON FILES OK"
  </verify>
  <done>plugins/ticket-workflow/ 경로가 완전히 삭제되고, 프로젝트 내 Python .py 파일이 0개</done>
</task>

</tasks>

<verification>
- `find plugins/wf -type f | sort` 실행 시 plugin.json + agents/7개 + SKILL.md + references/10개 파일 목록 출력
- `wc -l plugins/wf/skills/ticket-workflow/SKILL.md` 결과가 500 이하
- `ls plugins/ticket-workflow 2>&1`이 "No such file or directory" 반환
- `grep -r "lib/" plugins/wf/ --include="*.md"` 결과에 Python 모듈 참조 없음 (browser_automation.py, classifier.py 등)
</verification>

<success_criteria>
1. plugins/wf/ 디렉토리에 표준 플러그인 구조(.claude-plugin/plugin.json + agents/ + skills/ + references/) 완비
2. plugins/ticket-workflow/ 구 경로 완전 삭제
3. Python lib/*.py 참조가 모든 .md 파일에서 제거됨
4. v0.3 SKILL.md가 base로 유지되며 500줄 이하
</success_criteria>

<output>
After completion, create `.planning/phases/01-non-ui/01-01-SUMMARY.md`
</output>
