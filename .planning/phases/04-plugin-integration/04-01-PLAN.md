---
phase: 04-plugin-integration
plan: 01
type: execute
---

<objective>
Skill + Command 구조로 Multi-Agent Router를 Claude Code 플러그인으로 통합.

Purpose: 자연어 요청을 `/route` 커맨드로 적절한 AI에게 라우팅하는 플러그인 완성
Output: SKILL.md, commands/route.md
</objective>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/03-routing-logic/03-01-SUMMARY.md

**기존 자산:**
@multi-cli-runner.ts - CLI 실행 래퍼 (--auto 플래그)
@router.ts - 라우팅 로직
@result-extractor.ts - 결과 정규화

**검증된 흐름:**
1. 사용자 요청 → router.selectCLI() → CLI 선택
2. multi-cli-runner --auto → CLI 실행
3. result-extractor → 정규화된 결과 반환
</context>

<tasks>

<task type="auto">
  <name>Task 1: SKILL.md 작성</name>
  <files>SKILL.md</files>
  <action>
    Claude Code Skill 형식으로 플러그인 정의:

    1. 메타데이터:
       - name: multi-agent-router
       - description: 자연어 요청을 적절한 AI CLI로 라우팅
       - triggers: /route, "다른 AI에게", "codex로", "claude로"

    2. 기능 설명:
       - 자동 라우팅 (코드→codex, 일반→claude)
       - 수동 지정 가능 (/route --cli codex)
       - 결과 정규화 및 에러 처리

    3. 사용 예시:
       - /route "함수 작성해줘" → codex
       - /route "이 개념 설명해줘" → claude
  </action>
  <verify>
    test -f SKILL.md && head -20 SKILL.md
  </verify>
  <done>
    - SKILL.md 파일 생성됨
    - 트리거 및 기능 정의됨
  </done>
</task>

<task type="auto">
  <name>Task 2: /route 커맨드 구현</name>
  <files>commands/route.md</files>
  <action>
    Claude Code 커맨드 형식으로 /route 구현:

    1. 커맨드 메타데이터:
       - name: route
       - description: AI 라우터로 요청 전달
       - args: prompt (필수), --cli (선택)

    2. 실행 로직:
       - args 파싱
       - CLI 미지정 시 router.selectCLI() 호출
       - multi-cli-runner.ts 실행
       - 결과 반환

    3. 출력 형식:
       - 선택된 CLI 표시
       - AI 응답 내용
       - 메타데이터 (토큰, 비용, 시간)
  </action>
  <verify>
    test -f commands/route.md && head -20 commands/route.md
  </verify>
  <done>
    - commands/route.md 파일 생성됨
    - 커맨드 로직 정의됨
  </done>
</task>

<task type="auto">
  <name>Task 3: 통합 테스트</name>
  <files>multi-cli-runner.ts</files>
  <action>
    플러그인 동작 검증:

    1. echo mock으로 전체 흐름 테스트:
       node --import tsx multi-cli-runner.ts --auto "코드 작성" --test --normalize

    2. 실제 CLI 테스트 (선택적):
       node --import tsx multi-cli-runner.ts --auto "hello" --normalize
  </action>
  <verify>
    node --import tsx multi-cli-runner.ts --auto "test" --test --normalize
  </verify>
  <done>
    - 전체 플러그인 흐름 검증됨
    - 라우팅 → 실행 → 정규화 동작 확인
  </done>
</task>

</tasks>

<verification>
Before declaring phase complete:
- [ ] SKILL.md 존재 및 형식 준수
- [ ] commands/route.md 존재 및 형식 준수
- [ ] 전체 흐름 테스트 통과
</verification>

<success_criteria>
- Claude Code에서 /route 커맨드로 사용 가능한 구조
- 자동 라우팅 동작
- 결과가 정규화된 형식으로 반환
</success_criteria>

<output>
After completion, create `.planning/phases/04-plugin-integration/04-01-SUMMARY.md`
</output>
