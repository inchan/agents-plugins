---
phase: 01-cli-execution-poc
plan: 01
type: execute
---

<objective>
서브에이전트에서 외부 AI CLI(`codex exec`)를 실행하고 결과를 수집할 수 있는지 검증하는 최소 PoC 구현.

Purpose: Multi-Agent Router Plugin의 핵심 가설 검증 - "Claude Code 서브에이전트가 Bash를 통해 외부 AI CLI를 실행할 수 있는가?"
Output: `codex exec` CLI를 실행하고 결과를 JSON으로 반환하는 TypeScript 래퍼
</objective>

<execution_context>
@~/.claude/get-shit-done/workflows/execute-phase.md
@~/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md

**기존 자산:**
@agent.ts - 기존 claude CLI 실행 패턴 (spawnSync + JSON 파싱)

**Tech stack available:**
- Node.js + TypeScript (tsx)
- spawnSync from child_process (검증됨)

**Established patterns:**
- `spawnSync(cmd, args, { encoding: "utf-8" })` 패턴
- JSON 출력 파싱 (`safeJsonParse`)
- Report 타입 정의 및 성공/실패 판정

**핵심 가설:**
1. `codex exec` CLI가 설치되어 PATH에 있는가?
2. spawnSync로 실행 가능한가?
3. 결과를 파싱할 수 있는가?
</context>

<tasks>

<task type="auto">
  <name>Task 1: codex CLI 설치 확인 및 실행 테스트</name>
  <files>N/A (검증만)</files>
  <action>
    1. `which codex` 또는 `codex --version`으로 CLI 설치 확인
    2. 설치되지 않았다면 설치 방법 문서화 (npm install -g @openai/codex 등)
    3. 간단한 `codex exec "echo hello"` 명령 실행하여 동작 확인

    **주의:** codex CLI가 없을 경우, 다른 AI CLI (claude -p, 또는 단순 echo)로 대체하여 패턴만 검증
  </action>
  <verify>codex --version 또는 대체 CLI가 정상 실행됨</verify>
  <done>외부 AI CLI 실행 가능 여부 확인됨</done>
</task>

<task type="auto">
  <name>Task 2: Multi-CLI 래퍼 PoC 구현</name>
  <files>multi-cli-runner.ts</files>
  <action>
    기존 agent.ts 패턴을 참고하여 여러 AI CLI를 실행할 수 있는 래퍼 구현:

    1. CLI 설정 인터페이스 정의:
       ```typescript
       interface CLIConfig {
         name: string;           // "codex" | "claude" | "gemini"
         command: string;        // 실행 명령어
         args: string[];         // 기본 인자
         parseOutput: (stdout: string) => any;  // 출력 파싱
       }
       ```

    2. 기본 실행 함수:
       ```typescript
       function runCLI(config: CLIConfig, prompt: string): RunResult
       ```

    3. 최소 2개 CLI 설정 (claude, codex 또는 echo mock)

    **구현 시 주의:**
    - 기존 agent.ts의 spawnSync 패턴 재사용
    - 에러 핸들링: CLI 없음, 실행 실패, 파싱 실패 구분
    - 타임아웃 고려 (spawnSync의 timeout 옵션)
  </action>
  <verify>
    node --import tsx multi-cli-runner.ts --cli claude --prompt "say hello"
    node --import tsx multi-cli-runner.ts --cli echo --prompt "test message"
  </verify>
  <done>
    - multi-cli-runner.ts 파일 생성됨
    - 최소 1개 CLI 실행 성공
    - JSON 형식 결과 출력
  </done>
</task>

</tasks>

<verification>
Before declaring phase complete:
- [ ] multi-cli-runner.ts 파일 존재
- [ ] 최소 1개 외부 CLI 실행 성공 (codex, claude, 또는 echo mock)
- [ ] 실행 결과가 JSON 형식으로 출력됨
- [ ] TypeScript 컴파일 에러 없음 (tsx로 실행 가능)
</verification>

<success_criteria>

- 외부 CLI 실행 가능성 검증됨 (성공 또는 실패 원인 파악)
- 실행 패턴 코드화됨 (multi-cli-runner.ts)
- Phase 2로 진행 가능한 기반 구축
</success_criteria>

<output>
After completion, create `.planning/phases/01-cli-execution-poc/01-01-SUMMARY.md`:

# Phase 1 Plan 01: CLI 실행 검증 Summary

**[실행 결과 한줄 요약]**

## Accomplishments

- [검증 결과]
- [구현된 기능]

## Files Created/Modified

- `multi-cli-runner.ts` - Multi-CLI 래퍼 PoC

## Decisions Made

- [CLI 선택 관련 결정]
- [에러 핸들링 전략]

## Issues Encountered

- [발생한 문제와 해결책]

## Next Phase Readiness

- Phase 2 (결과 수집 검증) 준비 상태
- [필요한 추가 작업이 있다면]
</output>
