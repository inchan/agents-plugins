---
phase: 05-new-cli-integration
plan: 01
type: execute
---

<objective>
Gemini CLI와 Qwen Code CLI를 multi-cli-runner에 통합하고 라우팅 규칙 확장

Purpose: 라우팅 옵션 확장으로 다양한 AI CLI 선택 가능
Output: gemini, qwen CLI 설정 추가 + 라우팅 규칙 확장

**Scope Change:** 리서치 결과 Rovo Dev CLI는 non-interactive only로 통합 불가 → Gemini + Qwen 2개만 통합
</objective>

<execution_context>
@~/.claude/get-shit-done/workflows/execute-phase.md
@~/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/05-new-cli-integration/05-RESEARCH.md

**Key files:**
@.claude/skills/outsourcing/multi-cli-runner.ts
@.claude/skills/outsourcing/router.ts
@.claude/skills/outsourcing/result-extractor.ts

**Established patterns (from prior phases):**
- CLI_CONFIGS 객체에 새 CLI 추가 (multi-cli-runner.ts:62-102)
- DEFAULT_RULES 배열에 라우팅 규칙 추가 (router.ts:87-152)
- containsKeyword 함수로 키워드 매칭 (router.ts:68-71)

**Constraining decisions:**
- [Phase 1]: claude CLI는 --output-format json으로 구조화된 결과 반환
- [Phase 3]: 우선순위 기반 라우팅 규칙 (priority 높을수록 먼저 평가)
- [Research]: Gemini CLI는 --output-format json 지원, Qwen은 미지원
</context>

<tasks>

<task type="auto">
  <name>Task 1: Gemini CLI 설정 추가</name>
  <files>.claude/skills/outsourcing/multi-cli-runner.ts</files>
  <action>
CLI_CONFIGS 객체에 gemini 설정 추가:

```typescript
gemini: {
  name: "gemini",
  command: "gemini",
  buildArgs: (prompt: string) => [
    "-p", prompt,
    "--output-format", "json"
  ],
  parseOutput: safeJsonParse,
  timeout: 120000,
},
```

codex 설정 다음에 추가. Claude CLI와 동일한 패턴 (--output-format json 지원).
  </action>
  <verify>TypeScript 컴파일 에러 없음 (npx tsc --noEmit)</verify>
  <done>gemini CLI 설정이 CLI_CONFIGS에 추가됨</done>
</task>

<task type="auto">
  <name>Task 2: Qwen Code CLI 설정 추가</name>
  <files>.claude/skills/outsourcing/multi-cli-runner.ts</files>
  <action>
CLI_CONFIGS 객체에 qwen 설정 추가:

```typescript
qwen: {
  name: "qwen",
  command: "qwen",
  buildArgs: (prompt: string) => ["-p", prompt],
  parseOutput: (stdout: string) => ({ content: stdout.trim() }),
  timeout: 180000,  // 3분 - 480B 모델 지연 대비
},
```

gemini 다음에 추가. JSON 출력 미지원이므로 단순 텍스트 파싱.
주의: Qwen은 Gemini 포크지만 --output-format json 미지원.
  </action>
  <verify>TypeScript 컴파일 에러 없음</verify>
  <done>qwen CLI 설정이 CLI_CONFIGS에 추가됨</done>
</task>

<task type="auto">
  <name>Task 3: 라우팅 규칙 확장</name>
  <files>.claude/skills/outsourcing/router.ts</files>
  <action>
1. 키워드 상수 추가 (ANALYSIS_KEYWORDS 다음에):

```typescript
const GOOGLE_KEYWORDS = [
  'google', 'gmail', 'drive', 'sheets', 'docs', 'calendar',
  'gemini', 'vertex', 'firebase', 'gcp',
  '구글', '지메일', '드라이브', '시트', '캘린더',
];

const MULTILINGUAL_KEYWORDS = [
  'translate', 'translation', 'chinese', 'mandarin', 'korean',
  '번역', '중국어', '한국어', '다국어', 'multilingual',
  'qwen', 'alibaba',
];
```

2. DEFAULT_RULES에 새 규칙 추가 (code-tasks 다음, priority 85):

```typescript
// Google 관련 작업 - gemini 우선
{
  name: 'google-tasks',
  priority: 85,
  match: (req) => containsKeyword(req.prompt, GOOGLE_KEYWORDS),
  target: 'gemini',
  fallback: 'claude',
  description: 'Google 관련 작업은 gemini로 라우팅',
},

// 다국어/번역 작업 - qwen 우선
{
  name: 'multilingual-tasks',
  priority: 85,
  match: (req) => containsKeyword(req.prompt, MULTILINGUAL_KEYWORDS),
  target: 'qwen',
  fallback: 'claude',
  description: '다국어/번역 작업은 qwen으로 라우팅',
},
```
  </action>
  <verify>node --import tsx .claude/skills/outsourcing/router.ts "translate to chinese" 실행시 qwen 선택 확인</verify>
  <done>google-tasks, multilingual-tasks 규칙 추가됨</done>
</task>

</tasks>

<verification>
Before declaring phase complete:
- [ ] `npx tsc --noEmit` 에러 없음
- [ ] `node --import tsx .claude/skills/outsourcing/multi-cli-runner.ts --cli gemini --prompt "test"` 실행 (CLI 없으면 ENOENT 에러 정상)
- [ ] `node --import tsx .claude/skills/outsourcing/multi-cli-runner.ts --cli qwen --prompt "test"` 실행 (CLI 없으면 ENOENT 에러 정상)
- [ ] `node --import tsx .claude/skills/outsourcing/router.ts "google sheets api"` → gemini 선택
- [ ] `node --import tsx .claude/skills/outsourcing/router.ts "번역해줘"` → qwen 선택
</verification>

<success_criteria>
- CLI_CONFIGS에 gemini, qwen 설정 추가됨
- DEFAULT_RULES에 google-tasks, multilingual-tasks 규칙 추가됨
- 라우팅 테스트 통과
- Phase 5 완료 → v1.1 마일스톤 완료
</success_criteria>

<output>
After completion, create `.planning/phases/05-new-cli-integration/05-01-SUMMARY.md`:

# Phase 5 Plan 1: CLI 확장 Summary

**Gemini + Qwen CLI 통합 완료, Rovo Dev 제외**

## Accomplishments

- Gemini CLI 설정 추가 (JSON 출력 지원)
- Qwen Code CLI 설정 추가 (텍스트 파싱)
- Google 관련 라우팅 규칙 추가
- 다국어/번역 라우팅 규칙 추가

## Files Modified

- `.claude/skills/outsourcing/multi-cli-runner.ts` - gemini, qwen CLI 설정
- `.claude/skills/outsourcing/router.ts` - 라우팅 규칙 확장

## Decisions Made

- Rovo Dev CLI 제외 (non-interactive only, Atlassian 종속)

## Issues Encountered

[작업 중 발견된 이슈, 없으면 "None"]

## Next Step

Phase 5 완료 → v1.1 마일스톤 완료
</output>
